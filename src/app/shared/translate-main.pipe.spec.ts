import { TranslateMainPipe } from './translate-main.pipe';

describe('TranslateMainPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslateMainPipe();
    expect(pipe).toBeTruthy();
  });
});
